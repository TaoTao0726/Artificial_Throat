在完成前端硬件电路的搭建后，系统已能够将喉部的微弱振动稳定转化为PC端可处理的CSV电压时间序列。本章的核心任务是引入机器学习算法，实现对上述时序波形信号的自动特征提取与分类识别。

系统的算法部分主要基于Python语言与PyTorch深度学习框架进行开发。为了顺应深度学习算法的数据处理流向，整个分类识别程序被科学地划分为四个独立的核心模块，分别为数据预处理模块（dataset.py）、模型构建模块（model.py）、网络训练模块（train.py）以及推理预测模块（predict.py）。

在具体的工程实现环节中：首先由数据预处理模块解决真实采集场景下发声时长不一导致的数据对齐问题，完成张量格式的转换；其次，在模型构建模块中设计并搭建用于一维时域信号特征提取的卷积神经网络（1D-CNN）；随后，网络训练模块负责驱动该模型在已有数据集上进行反复的迭代优化，并导出收敛后的最优权重参数文件；最后，为满足系统的实时交互需求，推理预测模块通过直接加载预训练权重，实现对新采集波形信号的高效在线判别。本章后续内容将依次对上述四个核心环节的算法原理与工程逻辑展开详细阐述。

## 4.1 PC端机器学习算法的选择与构建

硬件采集电路将咽喉振动信号转化为电压时间序列后，PC端需要引入合适的机器学习模型对这些信号进行特征提取与分类识别。本节将结合采集数据的物理特性与实际工程需求，阐述PC端语音识别算法的选型依据，并详细介绍所构建的一维卷积神经网络（1D-CNN）的核心原理与网络架构。

### 4.1.1 算法选型依据

针对一维时间序列信号的分类任务，目前业界常用的方法主要包括传统机器学习算法、循环神经网络（RNN/LSTM）以及一维卷积神经网络（1D-CNN）。综合考虑本系统的硬件算力、数据规模以及开发周期，本文最终选用1D-CNN作为核心算法框架。传统机器学习算法在处理声带电压信号时，通常需要依赖快速傅里叶变换或小波变换等数学手段进行人工特征提取，工程实现较为繁琐且高度依赖经验。相比之下，卷积神经网络可以直接处理原始的一维电压序列，自动学习时域信号中的局部特征，实现了端到端的识别，大幅简化了数据预处理流程。

在深度学习框架内部，虽然长短期记忆网络（LSTM）等循环神经网络在长时序依赖的序列处理上表现优异，但其内部存在大量门控单元，参数规模极为庞大。考虑到本课题所采集的指令词汇数据集规模相对有限，盲目使用深层或复杂的LSTM极易发生过拟合现象，导致模型泛化能力下降。而1D-CNN的结构相对轻量，参数量较小，更适合中小规模数据集的稳定训练。此外，咽喉发声产生的压电薄膜信号中包含大量密集的波峰、波谷等局部突变特征，1D-CNN的滑动卷积核能够高效捕捉这些局部形态变化。得益于卷积层前向传播的高度并行计算特性，其模型推理速度远超依赖时间步串行计算的RNN网络，从而能够更好地满足上位机系统实时采集与预测的工程需求。

### 4.1.2 核心算法原理与模型构建

基于上述选型分析，本次毕业设计在PyTorch深度学习框架下构建了一个轻量级的一维卷积神经网络模型。为避免网络过深导致梯度消失或过拟合现象，模型整体结构被明确划分为特征提取模块与分类器模块两个部分，以实现从原始波形到抽象特征，再到最终分类标签的完整映射过程。

在特征提取模块中，模型输入端接收的是经过预处理后长度为6000的高维电压采样序列。为了对长序列进行快速降维并提取基础波形轮廓，网络的第一卷积层采用了尺寸较大的卷积核（核大小为15）和较大的步长（步长为2），并在其后连接了一维最大池化层。同时，为稳定网络中特征数据的分布并加速模型收敛，该层还引入了批归一化（BatchNorm）机制与ReLU非线性激活函数。随着网络结构逐渐加深、特征序列不断缩短，第二和第三卷积层换用了尺寸更小的卷积核（核大小分别为7和5），以便从浅层轮廓中进一步深挖更加细腻的局部高阶特征，并将特征通道数逐层扩展至32和64。在特征提取的末端，网络引入了自适应一维最大池化层（AdaptiveMaxPool1d），将前面不同通道内的特征图统一浓缩为单个特征数值。该操作不仅极大地压缩了后续全连接层的参数规模，有效降低了计算负荷，还显著提升了模型对信号时间轴上轻微偏移的容忍度。

经过前置的多次卷积与池化操作后，原始冗长的时序信号被转化为64维的高阶抽象特征向量，随后平铺输入至由线性全连接层构成的分类器模块。针对本系统设定的三个基础语音指令类别，分类器首先将64维特征映射至32维的隐藏层节点，经过ReLU激活函数处理后，再由最后一层全连接层输出维度为3的预测结果张量，其中数值最大的维度即对应模型最终的分类判定。为了进一步提升模型的泛化能力，保障其在实际在线测试中的准确率，分类器的隐藏层之间加入了一层失活率（Dropout Rate）设为0.3的Dropout网络层。该机制通过在训练迭代过程中随机断开部分神经元的连接，强制网络学习更加鲁棒的特征表示，从而有效防止了模型对有限训练集数据的过度拟合。

## 4.2 识别模型的训练与优化

### 4.2.1 数据集的构建与标签划分

在深度学习的训练流程中，规范的数据集构建与封装是保障模型高效学习的前提。本系统在PyTorch框架下，通过继承基础的Dataset类构建了专用的喉部语音数据集对象。考虑到前期硬件采集产生的大量波形数据均以独立的CSV文件格式离散存储，为了简化数据标签的管理，避免人工建立独立映射表带来的繁琐与错漏，本系统采取了基于文件名的自动标签解析策略。在上位机保存数据时，预先将对应的类别标号（如代表不同指令的0、1、2）作为文件名的前缀。在构建数据集对象时，程序会遍历指定的数据文件夹，利用字符串分割操作提取该前缀并转换为整型标签，从而实现了波形数据与真实分类标签的高效、准确对应。

完成标签解析与波形文件的读取后，程序将经过截断或补零操作以统一长度的Numpy数据数组，转化为PyTorch计算图所必须的浮点型张量格式。为了提升模型训练时的并行计算效率并稳定梯度下降的方向，本文进一步引入了DataLoader数据加载器机制，将训练的批大小设定为32。这意味着模型在每一次进行参数更新时，会同时参考32个样本的综合误差，有效缓解了单个极端样本对网络权重造成的剧烈冲击。同时，在数据加载器中开启了随机打乱功能，确保每个训练周期内输入数据的顺序都是随机的，从而消除数据排列特征对模型泛化能力造成的潜在负面干扰。

### 4.2.2 模型训练过程与参数调优

模型的训练过程本质上是一个寻找全局最小误差的迭代优化过程。针对本课题中的多类别分类任务，系统选用了深度学习中经典的交叉熵损失函数（Cross Entropy Loss）来衡量模型给出的预测概率分布与真实标签之间的差异程度。在网络优化器的选择上，本文采用了自适应矩估计优化器（Adam优化器），并将初始学习率设定为0.001。相较于传统的随机梯度下降算法，Adam优化器能够根据历史梯度的均值和方差自动动态调节每个参数的学习步长。这种特性使其不仅具备较快的收敛速度，还能有效避免模型在复杂的参数空间中陷入局部最优解。

在具体的代码实现中，系统设定的总训练轮数为20轮。在每一轮训练的批次循环内部，程序严格遵循着标准的参数更新逻辑：首先利用优化器的梯度清零函数清除上一批次残留的梯度信息，防止梯度累加导致优化方向偏移；随后，将批次张量数据输入至卷积神经网络中进行前向传播，获取网络对各个类别的预测打分；接着，通过交叉熵损失函数计算当前预测结果与真实标签的误差，并启动反向传播机制计算网络中所有可学习参数的梯度；最后，由Adam优化器根据计算出的梯度方向对网络权重进行微小调整。在整个训练过程中，程序会实时统计并输出每一轮的平均误差和准确率。当设定的20轮训练结束后，网络在训练集上的误差通常已收敛至平稳的极小值。此时，系统调用框架的保存接口，将网络当前最优的权重状态以独立文件（.pth）的形式进行导出并且保存，完成了从空白模型到具备识别能力升级，也为后续系统的在线实时预测奠定了基础。

## 4.3 人工喉系统整体闭环测试与性能评估

正文

### 4.3.1 发声动作的分类识别准确率分析

正文

### 4.3.2系统实时性与鲁棒性评估

正文

### 4.3.3 佩戴舒适度与防干扰能力评价

正文